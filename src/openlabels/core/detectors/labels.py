"""Canonical label mappings for ML detectors.

Maps model output labels to OpenLabels entity type taxonomy.
Stanford PHI label mappings are defined in phi_detector.py (STANFORD_PHI_LABEL_MAP).
GLiNER label mappings are defined in gliner.py (GLINER_LABEL_MAP).
"""

# Leave empty so requests use relative URLs and go through the Vite proxy,
# which forwards /api/* to localhost:8000 (see vite.config.ts).
# Setting this to http://localhost:8000 bypasses the proxy and breaks
# cookie handling (cross-origin).
VITE_API_URL=
